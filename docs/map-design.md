# AI 时代探索地图设计

## 浏览

时间总览 → 年度关键词与主线 → 事件 → 具体分支和来源。点选后保留返回路径；相关节点可以跨年跳转。

年度先显示少量主节点，一般事件按同期分支收拢。并行事件不强造因果。最终视觉支持缩放改变内容密度；本地初版先验证分层浏览，完整拖拽缩放和媒体专题逐步实现。

## 内容数据

`data/map.json` 为唯一内容源，使用 `schema_version` 标记契约。

| 对象 | 内容 |
|---|---|
| years | 年份、关键词、关键句、支撑节点、概括、覆盖状态和限制 |
| events | 稳定 ID、年份、日期及性质、标题、简述、领域、主次、入选理由、当时与后来看、来源和材料 |
| sources | ID、链接、标题、来源性质、发表日、查阅日与采用范围 |
| materials | 有出处的短引、报道或演示细节，不伪造截图或体验 |
| relations | 两端节点、关系类型、说明和证据，与界面展开层级分开 |

`tier` 为 main / branch / root；`roles` 为 milestone / public-impact / early-signal。前者是阅读主次，后者是入选理由，不是一套热度分数。

`then` 是同期认识及材料，`later` 是带后续证据与截止日期的回看。没有证据就不填确定结论。

`date` 用于排序；`date_label` 是显示日期，`date_precision` 和 `date_basis` 标明粒度与发生、发布或报道性质。月份的排序锚点不能展示为精确发生日。

## 年份与关系

主体为 2022—2026，逐年深化。每年分别标注本轮选材已复核、种子资料或阶段观察；这些状态都不代表全年覆盖完整。少量前史根节点单独浏览，不硬设完整年度主题。

关系类型：uses（采用）、extends（延续）、follow-up（后续）、context（背景关联）。采用/延续的 from 为使用者或后续方法，to 为被采用或前序方法；follow-up 从前事件指向后事件。前两类需要直接证据；context 不表示因果。没有关系的次级事件可以按时间进入同期折叠组。

## 更新

选材并核来源 → 定主次与入选理由 → 补当时或后续证据 → 检查年度概括与锚点 → 校验并构建。新增新闻不自动晋升主线；年度关键句随证据修订。

网页构建只读取 data/map.json，研究过程文件不参与打包。
